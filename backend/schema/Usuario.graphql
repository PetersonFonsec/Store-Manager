type Usuario {
    id: Int!
    nome: String!
    senha: String
    email: String
    ativo: Boolean
}

input UsuarioInput {
    id: Int
    nome: String!
    senha: String!
    email: String!
    ativo: Boolean
}

type UsuarioOutput {
    id: Int
    nome: String!
    email: String!
    ativo: Boolean
}


type loginOutput {
    id: Int
    nome: String!
    email: String!
    ativo: Boolean,
    token: String!
}

input UsuarioFiltro{
    email: String
    id: Int    
}