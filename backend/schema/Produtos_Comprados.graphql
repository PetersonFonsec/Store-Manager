type ProdutosComprados {
    compra_id: Int!
    produto_id: Int!
    quantidade: Int!
}

input ProdutosCompradosInput {
    compra_id: Int!
    produto_id: Int!
    quantidade: Int!
}

input ProdutosCompradosFiltro{
}