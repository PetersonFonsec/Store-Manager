type Venda {
    id: Int!
    total: Float!
    forma_pagamento: Int!
    cliente_id: Int!
}

input VendaInput {
    id: Int!
    total: Float!
    forma_pagamento: Int!
    cliente_id: Int!
}

input VendaFiltro{
    id: Int!
}