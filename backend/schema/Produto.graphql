type Produto {
    id: Int!
    nome: String!
    descricao: String!
    preco_compra: Float!
    preco_venda: Float!
    categoria_id: Int!
}

input ProdutoInput {
    id: Int!
    nome: String!
    descricao: String!
    preco_compra: Float!
    preco_venda: Float!
    categoria_id: Int!
}

input ProdutoFiltro{
    id: Int!    
}